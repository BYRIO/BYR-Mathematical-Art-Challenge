package work.byrio.byrmathematicalartchallenge;

import work.byrio.byrmathematicalartchallenge.model.V2;
import work.byrio.byrmathematicalartchallenge.model.V3;

/**
 * 渲染器类 (脚手架)
 * <br/>
 * 这个类是你参赛时使用的、允许修改的类。分别在 `red` `green`
 * `blue` 里填写你的渲染函数。渲染时，会在同一像素位置 (x, y)
 * 上分别调用这三个函数获取像素位置的颜色 (R, G, B)。
 *
 * @author YozakuraDoge
 */
public final class Render extends AbstractRender {
    public Render() {}

    public int red(int x, int y, double t) {
        // 在这里填写你的代码
        return 0;
    }

    public int green(int x, int y, double t) {
        // 在这里填写你的代码
        return 0;
    }

    public int blue(int x, int y, double t) {
        // 在这里填写你的代码
        return 0;
    }
}
